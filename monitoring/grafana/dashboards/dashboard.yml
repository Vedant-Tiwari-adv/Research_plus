apiVersion: 1

providers:
  - name: "Research+"
    orgId: 1
    folder: "Research+"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
